# Toy Chest 三消编辑器本地记忆

## 项目定位

这是一个单文件网页 Demo 编辑器，用来配置并试玩“玩具箱 / 玩具收纳分拣”三消玩法。直接打开 `index.html` 即可使用；开发验证时可通过本地静态 HTTP 服务访问。

## 核心玩法

- 玩具箱内只显示可点击的上层玩具；下层玩具只显示低饱和轮廓，不可点击。
- 点击一个上层玩具后，它进入固定 8 格暂存区，原锚点的下层玩具自然上浮成为新的上层。
- 暂存区出现 3 个相同元素时自动消除；现金元素 `id=0` 也遵循三消规则，完成后增加现金。
- 暂存区满时点击会被阻止，并提示使用道具。
- Demo 道具：撤回一步、自动清除、清空暂存槽。道具无限使用，用于模拟商业化关卡中的解困路径。

## 配置结构

编辑器通过 `game_level_control` 下发 JSON。当前 JSON 是数组或单对象，实际使用数组第一项。

```json
[
  {
    "Level": 1,
    "DefaultCount": 18,
    "InitialMatchCount": 2,
    "ForcedMatchCount": 2,
    "MaxConsecutiveSameItem": 2,
    "ItemControl": [
      {"Id": 1, "Count": 10, "Weight": "1:20,2:30,3:30"},
      {"Id": 2, "Count": 15, "Weight": "1:20,2:30,3:30"}
    ]
  }
]
```

字段规则：

- `Level`：展示优先级概念，数字越小优先；不是硬性关卡编号。所有配置完成后可循环使用最后 50 关的策略由业务层决定，Demo 只展示当前配置。
- `DefaultCount`：当前箱体的上层锚点数量 / 默认可见上层数量，Demo 最多使用 24 个锚点。
- `InitialMatchCount`：开局预置的三消组数。每组 3 个物品会拆分到 3 个不同锚点，确保它们在开局确实可点击完成匹配；实际数量不会超过 `DefaultCount`。
- `ForcedMatchCount`：暂存区剩余空位小于等于该值，且当前可见玩具 + 暂存区没有三消机会时，尝试从当前阶段队列中挑选可形成匹配的元素。Demo 记录触发次数，不保证所有极端配置都能强制成功。
- `MaxConsecutiveSameItem`：队列洗牌时限制同一元素的最大连续出现次数。例如配置为 2 时允许 `A,A,B`，不允许 `A,A,A`。
- `ItemControl`：按 `Id` 升序执行的阶段列表。
  - `Count` 是消除组数，每组 3 个物品，因此阶段总物品数为 `Count * 3`。
  - `Weight` 是 `元素id:相对权重` 逗号分隔字符串，支持元素 `0-12`。按最大余数法分配到完整三消组，组内不会拆散。
  - 阶段切换发生在上一阶段的生成队列全部进入上层 / 下层展示队列后，不代表该阶段物品已全部消除。

## 生成流程

1. 每个阶段按 `Weight` 将 `Count` 分配为若干完整三消组。
2. 阶段内部用带连续次数限制的随机洗牌生成队列。
3. 从阶段队列中抽取 `InitialMatchCount` 组，拆成 3 个独立上层锚点。
4. 先填满所有上层锚点，避免为了制造下层轮廓而把后续阶段元素提前放到可点击上层。
5. 上层填满后，再用剩余队列填充各锚点的下层轮廓（每个锚点最多两层）。
6. 试玩过程中，点击后只更新对应锚点；新物品从队列进入该锚点，不对其他锚点重新排序。

## 空间布局与视觉约束

- `makeAnchorLayout(count)` 生成覆盖箱体的行列骨架，按数量选择 4、5 或 6 列。
- 每次 `build()` 都对锚点进行新的横向 / 纵向扰动、旋转、缩放和锚点顺序随机化，因此每局位置不同，同时仍保持居中、分散和整体覆盖。
- `anchorPixel(anchor, depth)` 将 0-1 归一化锚点转换为 `toy-layer` 内的安全像素坐标，并把上层和下层都限制在容器边界内。
- `toy-layer` 是箱内裁切层；玩具尺寸和边距在 `anchorPixel` 中统一计算，避免超出红框。
- `ResizeObserver` 在容器尺寸变化时重新渲染坐标，支持响应式宽度。
- 下层通过低透明度、低饱和度和堆叠偏移表现纵深；不参与点击事件。

## 主要运行状态

`game` 主要字段：

- `total`：配置中的总消除组数。
- `cleared`：已完成三消的组数。
- `tray`：暂存区物品数组，最大长度 8。
- `anchors`：锚点及其 `stack`，`stack[0]` 是上层可点击物品。
- `queue`：尚未进入箱体的生成队列。
- `phaseMap`：阶段 Id 映射。
- `forced` / `lastForced`：强制匹配统计和最近一次是否触发。
- `complete`：所有组完成后的通关状态。

## 维护注意事项

- 修改布局时必须同时检查 `makeAnchorLayout` 和 `anchorPixel`，不要重新引入像素坐标与归一化坐标混用。
- 任何新的堆叠偏移都需要对旋转后的视觉外接矩形做边界测试。
- `DefaultCount` 低于 `InitialMatchCount * 3` 时，初始组数会被实际容量限制。
- 阶段物品总数必须是完整三消组的倍数；不要在阶段生成后随意删除单个物品。
- 这是“带道具诱导”的试玩 Demo，不以理论上每条路径必解为目标；强制匹配和道具用于调节体验与商业化压力。

## 本地验证

```bash
python3 -m http.server 8128
open http://127.0.0.1:8128/index.html
```

建议验证：上层 / 下层数量、玩具边界、点击后的暂存与补位、随机重开局差异、控制台错误。
